package kz.aitu.additional;

public class mainTest {
  /*  public static void main(String[] args) {
        Stack first = new Stack();
        Stack second = new Stack();
        Stack third = new Stack();
        Stack firstn = new Stack();
        Stack secondn = new Stack();
        Stack thirdn = new Stack();
        Stack laster = new Stack();




        first.Push(1);
        first.Push(2);
        first.Push(12);
        first.Push(18);
        first.Push(19);
        while (first.size()!=0){
            firstn.Push(first.pop());
        }


        //second push
        second.Push(3);
        second.Push(4);
        second.Push(15);
        second.Push(18);

        while (!second.isEmpty()){
            secondn.Push(second.pop());
        }


        //third push

        third.Push(5);
        third.Push(6);
        third.Push(20);
        third.Push(30);
        third.Push(35);
        third.Push(40);

        while (!third.isEmpty()){
            thirdn.Push(third.pop());
        }
        System.out.println(first.peak());
        System.out.println(" dawd" +firstn.peak());
        thirdn.souter();
        System.out.println("poshla rodimaya");
        int fi,se,th;
        do {
            fi=firstn.peak();
            se=secondn.peak();
            th=thirdn.peak();


            if (fi<=se&&fi<=th&&fi!=0){
                laster.Push2(firstn.pop());
            }
            else if(se<=fi && se<=th&&se!=0){

                laster.Push2(secondn.pop());
            }
            else if (th<=se&&th<=fi&&th!=0){
                laster.Push2(thirdn.pop());
            }



        }
        while(!firstn.isEmpty()&&!second.isEmpty()&&!thirdn.isEmpty());
        System.out.println("poshla rodimaya");
        laster.souter();

    }*/




}
